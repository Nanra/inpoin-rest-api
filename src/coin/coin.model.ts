export class Coins {
    constructor (
        public fromCoin: string, 
        public fromCoinBalance: number,
        public toCoin: string,
        public coinExchange: number, 
        ) {}
}